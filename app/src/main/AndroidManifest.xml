<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.madproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity android:name=".Add_form" />
        <activity android:name=".expenseHome" />
        <activity android:name=".display_count" />
        <activity android:name=".history" />
        <activity android:name=".select_meal" />
        <activity android:name=".diet" />
        <activity android:name=".Add_ToDo" />
        <activity android:name=".BMImain" />
        <activity android:name=".Calculate" />
        <activity android:name=".BmiList" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FoodList"
            android:label="Food List"
            android:parentActivityName=".MainActivity" />
    </application>

</manifest>